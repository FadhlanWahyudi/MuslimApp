<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.muslimapp"><uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TakbirActivity">
        </activity>
        <activity android:name=".TahmidActivity"/>
        <activity android:name=".JadwalActivity"/>
        <activity android:name=".QiblatActivity"/>
        <activity android:name=".DetailDzikir"/>
        <activity android:name=".Tasbeeh"/>
        <activity android:name=".AsmaulHusna"/>
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SettingActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ScreenActivity" />
        <activity android:name=".MainActivity" />
        <receiver android:name=".AlarmReceiver"
            android:process=":remote"/>
    </application>

</manifest>